# Application Environment
# ==================
# IMPORTANT: Never commit the actual .env file to version control
# Use this .env.example as a template and create a separate .env file for each environment

# Flask Environment (development, testing, or production)
FLASK_ENV=development
FLASK_DEBUG=0  # Always 0 in production for security

# Security
# ========
# CRITICAL: Generate a secure random key for production
# Use: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=replace-with-secure-random-key-at-least-32-chars

# Database
# ========
# Development: SQLite is fine
DATABASE_URL=sqlite:///instance/site.db
# Production: Use proper database URL
# DATABASE_URL=postgresql://user:password@localhost/dbname
# Test database (for running tests)
TEST_DATABASE_URL=sqlite:///instance/test.db

# Mail Configuration
# =================
# IMPORTANT: Use app-specific passwords for Gmail
# Enable 2FA and generate an app password at: https://myaccount.google.com/apppasswords
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-digit-app-specific-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Admin User
# =========
# CRITICAL: Change these in production
# Password should be at least 12 characters with mixed case, numbers, and symbols
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-secure-password

# Sitemap and URLs
# ===============
# Development can use HTTP
SITEMAP_URL_SCHEME=http
SITEMAP_BASE_URL=http://localhost:5000

# Production must use HTTPS
# SITEMAP_URL_SCHEME=https
# SITEMAP_BASE_URL=https://your-domain.com